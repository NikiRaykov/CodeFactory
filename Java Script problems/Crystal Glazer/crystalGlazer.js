const numberOfChildren = prompt("Enter the number of children: ");
const name = prompt("Enter the name: ");
const getLocation = ("Enter the location: ");
const jobTitle = ("Enter the job title: ");

function crystalGlazer(numberOfChildren, name, getLocation, jobTitle) {
    console.log(`You will be a ${jobTitle} in ${getLocation}, and married to ${name} with ${numberOfChildren} kids.`)
}


crystalGlazer(numberOfChildren, name, getLocation, jobTitle);

